#include <stdio.h>
int main() 
{
	double result; /*The result of the division*/
	int a = 5;
	int b = 13.5;
	result = a / b;
	printf("The result is %f\n", result);
	return 0;
}
/*The format '%f' expects argument of type 'double' but argument 2 has type 'int'*/

